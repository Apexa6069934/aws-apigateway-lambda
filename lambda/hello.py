import json

def handler(event, context):
    print('request: {}'.format(json.dumps(event)))
    return {
        'statusCode': 200,
        'headers': {
            'Content-Type': 'text/plain'
        },
        'body': 'You have successfully deployed the use case for implementing an Amazon API Gateway REST API connected to an AWS Lambda function pattern'
    }